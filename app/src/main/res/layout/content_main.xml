<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/content_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="com.example.parth.bhukad.MainActivity"
    tools:showIn="@layout/activity_main"
    android:background="@android:color/black">

    <ListView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/food_list_view"
        android:layout_above="@+id/deleter" />

    <Button
        android:text="clear"
        android:layout_width="150dp"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true"
        android:id="@+id/deleter"
        style="@style/Widget.AppCompat.Button.Borderless"
        android:onClick="Cleaner_function"
        android:textColor="@android:color/holo_purple" />

    <Button
        android:text="add"
        android:layout_height="wrap_content"
        android:id="@+id/Database_addder"
        android:layout_width="150dp"
        style="@style/Widget.AppCompat.Button.Borderless"
        android:onClick="Adder_function"
        android:textColor="@android:color/holo_purple"
        android:layout_alignParentBottom="true"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true" />
</RelativeLayout>
